const config = {
  // Tailwind v4 requires using the `@tailwindcss/postcss` plugin
  // instead of the old `tailwindcss` entry.
  plugins: ['@tailwindcss/postcss', 'autoprefixer'],
};

export default config;
