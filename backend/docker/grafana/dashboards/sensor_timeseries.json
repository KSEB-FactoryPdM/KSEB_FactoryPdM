{
  "id": null,
  "uid": "unity-sensor-data",
  "title": "Unity Sensor Data",
  "folder": "Sensors",
  "schemaVersion": 27,
  "version": 1,
  "refresh": "5s",
  "time": {"from": "now-1h", "to": "now"},
  "templating": {
    "list": [
      {
        "name": "device",
        "type": "query",
        "label": "Device",
        "datasource": {"type": "postgres", "uid": "TimescaleDB"},
        "query": "SELECT DISTINCT device FROM sensor_data ORDER BY device",
        "refresh": 2,
        "includeAll": true,
        "multi": true,
        "allValue": ".*",
        "current": {"text": "All", "value": "$__all"}
      }
    ]
  },
  "panels": [
    {
      "type": "timeseries",
      "title": "$device Current (x,y,z)",
      "gridPos": {"h": 10, "w": 24, "x": 0, "y": 0},
      "repeat": "device",
      "maxPerRow": 2,
      "datasource": {"type": "postgres", "uid": "TimescaleDB"},
      "targets": [
        {"format": "time_series", "rawQuery": true, "refId": "X", "alias": "x",
         "sql": "SELECT time AS \"time\", value AS \"value\", 'x' AS metric FROM sensor_data WHERE $__timeFilter(time) AND sensor_type='x' AND device = ${device:singlequote} ORDER BY 1"},
        {"format": "time_series", "rawQuery": true, "refId": "Y", "alias": "y",
         "sql": "SELECT time AS \"time\", value AS \"value\", 'y' AS metric FROM sensor_data WHERE $__timeFilter(time) AND sensor_type='y' AND device = ${device:singlequote} ORDER BY 1"},
        {"format": "time_series", "rawQuery": true, "refId": "Z", "alias": "z",
         "sql": "SELECT time AS \"time\", value AS \"value\", 'z' AS metric FROM sensor_data WHERE $__timeFilter(time) AND sensor_type='z' AND device = ${device:singlequote} ORDER BY 1"}
      ]
    },
    {
      "type": "timeseries",
      "title": "$device Vibration (vibe)",
      "gridPos": {"h": 10, "w": 24, "x": 0, "y": 10},
      "repeat": "device",
      "maxPerRow": 2,
      "datasource": {"type": "postgres", "uid": "TimescaleDB"},
      "targets": [
        {"format": "time_series", "rawQuery": true, "refId": "V", "alias": "vibe",
         "sql": "SELECT time AS \"time\", value AS \"value\", 'vibe' AS metric FROM sensor_data WHERE $__timeFilter(time) AND sensor_type='vibe' AND device = ${device:singlequote} ORDER BY 1"}
      ]
    }
  ]
}


