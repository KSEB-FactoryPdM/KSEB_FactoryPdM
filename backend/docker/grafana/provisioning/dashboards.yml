apiVersion: 1

providers:
  - name: "Sensors Provider"
    orgId: 1
    folder: "Sensors"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards
      foldersFromFilesStructure: true